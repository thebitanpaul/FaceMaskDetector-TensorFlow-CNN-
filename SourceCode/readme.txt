Here you ca find the python code to train the CNN model named as "FaceMaskRecognition.py"
and then the python code to activate the camera and apply the model named as "DetectMaskVideo.py"
and the required libraries named as "requirments.txt"
